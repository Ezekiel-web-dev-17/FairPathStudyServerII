version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: fair-path-redis
    restart: always
    ports:
      - '127.0.0.1:6379:6379'
    volumes:
      - redis_data:/data

volumes:
  redis_data:
